name: LoL Gemini Auto Post

on:
  schedule:
    - cron: '0 0,12 * * *'   # 日本時間 朝9時 と 夜9時
  workflow_dispatch:         # 手動実行ボタンも付く

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot
        env:
          GEMINI_API_KEY: ${{ AIzaSyDcTzliAbBT022tZQFQilJ7rHH8UPEwKzU }}
          X_CONSUMER_KEY: ${{ KqqGOWPH7C2NZNbBueh9RnXBf }}
          X_CONSUMER_SECRET: ${{ xnX1XzfsKAqhO6atccRV1vpTTj06fnX9e7Ll1NDoJgonZQQmLB }}
          X_ACCESS_TOKEN: ${{ 1141352675037995008-hvIpt75VXyC8uhkRDu0sSERnakSNhF }}
          X_ACCESS_TOKEN_SECRET: ${{ vCe001mD6EHGrzwjbxAl1h9AWQmLhOjAm7EvNn84jd9c8 }}
        run: python main.py
